{
  "name": "@tiny-web-builder/api",
  "version": "1.0.0",
  "description": "Cloudflare Worker API for tiny-web-builder",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --port 8787 --persist-to=./.wrangler/state",
    "build": "wrangler deploy",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "hono": "^3.12.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.3.0",
    "wrangler": "^4.0.0"
  }
}
